{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *.yml|*.yaml) command -v yamllint >/dev/null 2>&1 && yamllint \"$f\" || true;; esac; } 2>/dev/null || true",
            "timeout": 15,
            "statusMessage": "yamllint..."
          }
        ]
      }
    ]
  }
}
